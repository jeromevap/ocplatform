<ul class="nav">
    {% for advert in listAdverts %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('oc_advert_view', {'id': advert.id}) }}">{{ advert.title }}</a>
    </li>
    {%  endfor %}
</ul>

