{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} - {{ post.title }}{% endblock %}

{% block body %}
    <div class="col-12">
        <p>{{ post.title }}</p>
        <p>{{ post.author }}</p>
        <p>{{ post.content }}</p>
        <p>{{ post.date | date('d/m/Y') }}</p>
    </div>
    <p>
        <a href="{{ path('oc_advert_index') }}" class="btn btn-outline-primary">
            <i class="fa fa-chevron-left"></i>
            Retour à la liste
        </a>
        <a href="{{ path('oc_advert_edit', {'id': post.id}) }}" class="btn btn-outline-primary">
            <i class="fa fa-edit"></i>
            Modifier l'annonce
        </a>
        <a href="{{ path('oc_advert_delete', {'id': post.id}) }}" class="btn btn-outline-danger">
            <i class="fa fa-trash"></i>
            Supprimer l'annonce
        </a>
    </p>
{% endblock %}
