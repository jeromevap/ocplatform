{% extends 'layout.html.twig' %}


{% block title %}{{ parent() }} - Index{% endblock %}

{% block body %}
    <div class="row">
        {% for advert in listAdverts %}
            <div class="col-md-4">
                <a href="{{ path('oc_advert_view', {'id': advert.id}) }}"><h5>{{ advert.title }}</h5></a>
                <p>Auteur : {{ advert.author }}</p>
            </div>
        {% endfor %}
    </div>

{% endblock %}

