{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} - Liste des candidatures {% endblock %}

{% block body %}
    <div class="col-12">
        <dl>
            {% for app in applications %}
                <dt>{{ app.content }}</dt>
                <dd>{{ app.author }}</dd>
            {% endfor %}
        </dl>
    </div>
{% endblock %}

